# --- Dependencies ---
node_modules/
# You should generally KEEP package-lock.json so dependencies are consistent for others.
# If you really want to ignore it, uncomment the line below:
# package-lock.json

# --- Frontend Build ---
dist/
build/
*.local

# --- Backend Uploads & Storage ---
server/uploads/
uploads/
server/test.jpg  # Based on your tree, this looks like a temp testing file

# --- Python / AI ---
venv/
__pycache__/
*.py[cod]
# Ignore the model file if it is standard (download on install)
# If this is a CUSTOM model you trained, you might want to remove this line so you can push it.
*.pt

# --- Security (CRITICAL) ---
.env
.env.*
*.pem
*.key
*.crt
# Explicitly ignoring the ones found in your tree
cert.pem
key.pem

# --- Logs ---
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# --- Editor / System ---
.vscode/
.idea/
.DS_Store
*.swp

# --- Docker (Optional) ---
# Usually you DO want to commit these, but if you want to ignore them:
# docker-compose.yml
# Dockerfile